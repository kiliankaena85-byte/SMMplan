'use client';
export function ReferralEconomicsChart({ paidOut, pending }: { paidOut: number, pending: number }) {
  return (
    <div className="h-[200px] w-full mt-4 flex items-center justify-center bg-slate-50 border border-dashed rounded-lg">
      <span className="text-slate-400 font-medium">Чарты временно отключены</span>
    </div>
  );
}
