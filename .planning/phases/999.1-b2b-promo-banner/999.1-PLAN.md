---
wave: 1
depends_on: []
files_modified:
  - src/components/landing/WhyUs.tsx
autonomous: true
requirements: [B2B-BANNER-01]
---

# Phase 999.1: B2B Promo Banner on Landing (Frictionless Acquisition)

## Execution Tasks

<task>
<id>1</id>
<title>Rearrange and Update Bento Grid Layout in WhyUs.tsx</title>
<read_first>
- src/components/landing/WhyUs.tsx
</read_first>
<action>
Modify [WhyUs.tsx](file:///d:/SMM_plan_2/src/components/landing/WhyUs.tsx) to restructure the Bento Grid layout to include the high-contrast B2B Reseller Suite double card:
1. **Layout Grid Spans:**
   - Card 1: `md:col-span-2` -> AI-подбор услуг (Existing, keep but adapt if needed).
   - Card 2: `md:col-span-1` -> Прозрачные условия (Move former Card 3 to Card 2, small span).
   - Card 3: `md:col-span-1` -> VIP Лояльность / Персональные скидки (Compress former Card 4 `col-span-2` to a small `col-span-1` card. Change title to "Персональные скидки" and simplify copy).
   - Card 4: `md:col-span-2` -> **B2B Кабинет Реселлера** (New premium double-span card with custom dark high-contrast style).
2. **Styling B2B Card (Card 4):**
   - Theme: Slate-900 / dark aesthetic. Use:
     `bg-slate-900/90 dark:bg-slate-950/90 border border-indigo-500/20 text-white rounded-[2rem] p-8 relative overflow-hidden group shadow-2xl transition-all duration-300 hover:shadow-indigo-500/10`
   - Background Accent Blur:
     Add a glowing circle: `absolute top-0 right-0 w-80 h-80 bg-gradient-to-br from-indigo-500/20 to-violet-500/20 rounded-full blur-3xl opacity-60 group-hover:opacity-90 group-hover:scale-110 transition-all duration-500 -translate-y-1/3 translate-x-1/3`
   - Background Grid Mesh:
     Add absolute mesh layout for that premium feeling:
     `<div className="absolute inset-0 bg-[linear-gradient(to_right,#ffffff03_1px,transparent_1px),linear-gradient(to_bottom,#ffffff03_1px,transparent_1px)] bg-[size:16px_16px] [mask-image:radial-gradient(ellipse_60%_50%_at_50%_0%,#000_70%,transparent_100%)] opacity-30 pointer-events-none" />`
3. **Triple-Hook Highlights:**
   Display features with gorgeous inline layouts and icons (`IconSparkles`, `IconShieldCheck`, `IconTrendingUp`, etc.):
   - **Массовый заказ:** Умный Excel-парсер с автоматической очисткой и валидацией ссылок.
   - **PerfectPanel API v2:** Интеграция по стандартному протоколу PerfectPanel API для автоматизации.
   - **Wholesale-скидки:** Накопительный дисконт до 15% на весь каталог услуг пожизненно.
4. **Reseller CTA Button:**
   Implement a stunning glowing CTA button linking directly to the registration path:
   ```tsx
   import Link from "next/link";
   import { IconArrowUpRight } from "@tabler/icons-react";
   
   // Inside the Card 4 JSX:
   <div className="mt-8 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-6 relative z-10">
     <div className="text-left">
       <span className="text-xs font-bold text-indigo-400 uppercase tracking-widest block mb-1">Специальные условия</span>
       <span className="text-sm text-slate-300">Начните работу без обязательств по минимальному обороту.</span>
     </div>
     <Link 
       href="/auth/register?role=reseller&promo=B2BSTART"
       className="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 hover:from-indigo-600 hover:via-purple-600 hover:to-pink-600 text-white text-sm font-bold shadow-lg shadow-indigo-500/25 transition-all duration-200 hover:scale-[1.02] active:scale-[0.98]"
     >
       <span>Личный кабинет B2B</span>
       <IconArrowUpRight className="w-4 h-4" />
     </Link>
   </div>
   ```
</action>
<acceptance_criteria>
- src/components/landing/WhyUs.tsx includes a grid containing four items.
- One card includes the Link element pointing directly to '/auth/register?role=reseller&promo=B2BSTART'.
- The card contains the text 'PerfectPanel API v2'.
- The card contains the text 'Массовый заказ'.
</acceptance_criteria>
</task>

<task>
<id>2</id>
<title>Verify Compilation and Zero Hydration Errors</title>
<read_first>
- src/components/landing/WhyUs.tsx
- package.json
</read_first>
<action>
Execute compilation checks to verify the changes:
1. Run `npx tsc --noEmit` from the project root to ensure complete type safety with the new imports.
2. Run `npm run build` to confirm Next.js static page generation and server side rendering complete without errors.
</action>
<acceptance_criteria>
- TypeScript compiler output contains 0 errors (Exit code: 0).
- Next.js production build completes successfully (Exit code: 0).
</acceptance_criteria>
</task>

## Verification Strategy

- **Static Validation:** Verify the presence of B2B parameters and premium styling classes directly via string matching.
- **Compilation Gate:** Ensure zero type errors and build compliance.
- **DoD Checklist Alignment:** Verify styling classes follow globals.css tokens, transitions use duration-200/300.
