# 🔧 DEBT Report — Smmplan (2026-07-04)

## 1. Анализ кодовой базы и качество кода
* **TypeScript Strictness:** Проект успешно проходит полную проверку типов (`npx tsc --noEmit` завершился без ошибок).
* **Спецификация зависимостей:** Стек технологий полностью актуален (Next.js 16, React 19, Tailwind CSS v4, Prisma 5, HeroUI v3).

## 2. Анализ статических проверок (ESLint)
В ходе сканирования `npx eslint src/` выявлено **40 проблем (errors)**. 

### Основные категории техдолга:
1. **Использование `any` типа (31 ошибка `@typescript-eslint/no-explicit-any`):**
   * Выявлено в Server Actions настроек, операторских действиях с заказами и тикетами, а также в табличных компонентах.
   * *Влияние:* Ослабляет статическую типизацию TypeScript и может приводить к runtime-ошибкам при изменении контрактов данных.
2. **Неиспользуемые импорты и переменные (9 ошибок `@typescript-eslint/no-unused-vars`):**
   * Обнаружены в `OrderDrawer.tsx`, `operator-topbar.tsx`, а также в легал-сервисах и страницах тикетов/транзакций.
   * *Влияние:* Засорение кода неиспользуемыми импортами и объявленными константами.

## 3. Рекомендации по устранению
* **Рефакторинг `any`:** Заменить все заглушки `any` на строгие TypeScript типы и интерфейсы (особенно в Server Actions).
* **Автоматическое исправление:** Запустить `npx eslint src/ --fix` для удаления неиспользуемых импортов и переменных, а также настроить pre-commit хуки (Husky + lint-staged).
