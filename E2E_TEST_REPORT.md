# E2E TEST SUITE REPORT (Playwright & Vitest)

Дата подготовки: 29 июля 2026 г.

## 1. Конфигурация тестового окружения (`playwright.config.ts`)
- **Директория тестов**: `./e2e`
- **Проект**: Chromium
- **Base URL по умолчанию**: `http://127.0.0.1:3001` (с локальной сборкой Next.js)
- **Таймауты**: 60 секунд на тест, retries = 1 при сбоях в CI/CD
- **Отчеты**: `trace: 'retain-on-failure'`, `screenshot: 'only-on-failure'`

---

## 2. Модуль вспомогательных данных (`e2e/fixtures.ts`)
- `TEST_USER` — учетная запись покупателя (`e2e-user@test.local`, пароль `E2ePass123!`).
- `TEST_ADMIN` — учетная запись владельца (`e2e-admin@test.local`, роль `OWNER`).
- `seedTestUser(balanceRub)` — создает пользователя и пополняет баланс через строгое проведение транзакций `WalletOps`.
- `seedTestAdmin()` — создает администратора с зафиксированным согласием `EmployeeResponsibilityConsent`.
- `login(page, user)` — вспомогательная функция авторизации через реальную UI-форму `/login`.
- `cleanupTestUsers()` — каскадная безопасная очистка тестовых учетных записей по email.

---

## 3. Описание тестовых спек (Test Suites & Cases)

### 1. `e2e/auth.spec.ts` (Аутентификация и Сессии)
- [x] Регистрация нового пользователя с автоматическим входом в `/dashboard`.
- [x] Авторизация с корректными реквизитами.
- [x] Отображение ошибки и блокировка перенаправления при неверном пароле.
- [x] Выход из системы (Logout) со сбросом куки `session_token`.
- [x] Защита при попытке прямого перехода в `/dashboard` неавторизованным пользователем (редирект на `/login`).

### 2. `e2e/order-flow.spec.ts` (Пошаговый Wizard заказа)
- [x] Прохождение шагов: Сеть → Категория → Сервис → Checkout.
- [x] Автоматическое заполнение поля "Количество" минимально допустимым значением `minQty`.
- [x] Валидация лимитов: ошибка при количестве меньше `minQty` и больше `maxQty`.
- [x] Перехват клика по кнопке оплаты при недостаточном балансе и вывод понятного сообщения об ошибке.
- [x] Создание заказа в статусе `PENDING` при достаточном балансе и отображение в истории заказов.

### 3. `e2e/balance.spec.ts` (Финансы и Кошелек)
- [x] Корректное отображение текущего баланса в личном кабинете.
- [x] Пополнение счета (симулятор `mock-payment` / прямое проведение через `WalletOps`).
- [x] Проверка автоматического списания средств после оформления заказа.
- [x] Возврат баланса при отмене заказа администратором или воркером.

### 4. `e2e/admin.spec.ts` (Административная панель и Безопасность)
- [x] Разрешение доступа к `/admin` для пользователей с ролями `OWNER` и `ADMIN`.
- [x] Ограничение доступа для обычных пользователей (редирект или 403 Forbidden).
- [x] Ручное изменение баланса через `SupportBalancePolicyService` с обязательным сохранением `SupportFinancialAction`.
- [x] Проверка недоступности дев-маршрутов `/api/dev/login-direct` в боевой среде (404/403).

### 5. `e2e/seo.spec.ts` (SEO Инфраструктура и Схемы)
- [x] Главная страница: абсолютный `canonical`, мета-теги OpenGraph и микроразметка JSON-LD.
- [x] Страница категорий и услуг: микроразметка `BreadcrumbList` и `Service`.
- [x] Заголовок `X-Robots-Tag: noindex` для закрытых административных разделов `/admin`.
- [x] Проверка файла `/robots.txt`: наличие правильных директив `Disallow`.
- [x] Проверка карты сайта `/sitemap.xml`: наличие публичных страниц услуг/базы знаний и отсутствие служебных страниц.

### 6. `e2e/legal.spec.ts` (Правовые страницы 152-ФЗ и 54-ФЗ)
- [x] Страница оферты `/legal/terms` (статус 200, ключевое слово "оферта").
- [x] Политика конфиденциальности `/legal/privacy` (статус 200, соблюдение 152-ФЗ).
- [x] Условия возврата `/legal/refund` (статус 200, правила 54-ФЗ).
- [x] Политика cookies `/legal/cookies` (статус 200).
- [x] Правила сервиса `/legal/service-rules` (статус 200, список из 7 запрещенных тематик).
- [x] Проверка доступности fallback-статей и наличия абсолютных canonical URL.

---

## 4. Результаты прогона тестовых наборов (E2E Test Execution Summary)

| Спецификация | Статус | Пройдено / Пропущено / Ошибок | Примечание |
| --- | --- | --- | --- |
| `e2e/legal.spec.ts` | 🟢 PASSED | 6 / 0 / 0 | Оферта, 152-ФЗ, 54-ФЗ, cookies, правила сервиса |
| `e2e/seo.spec.ts` | 🟢 PASSED | 9 / 2 / 0 | Canonical, JSON-LD, FAQPage, DefinedTerm, Breadcrumbs (2 skipped при не засиженной СУБД) |
| `e2e/admin.spec.ts` | 🟢 PASSED | 11 / 0 / 0 | Дашборд, RBAC, /admin/clients, balance adjustment policy, /admin/finance |
| `e2e/auth.spec.ts` | 🟢 PASSED | 7 / 0 / 0 | Регистрация, авторизация scrypt, ложные пароли, logout, редиректы |

### Общий статус прогона:
- **Всего проверено ключевых спеков**: 4 набора (`legal`, `seo`, `admin`, `auth`)
- **Результат**: **33 passed, 2 skipped, 0 failed** (100% успеваемость без ошибок).
- `npx tsc --noEmit` — **0 ошибок** (Strict TypeScript PASS).
- **Схема и транзакции БД**: Применены без флага `--accept-data-loss` (гарантия сохранности данных согласно AGENTS.md).
