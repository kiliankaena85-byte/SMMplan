node_modules
.next
.env
.env.bak
.env.local
.env.production
.env.development
*.tsbuildinfo

# ── Staging env (contains real secrets — treat as compromised) ──
.env.staging
.env.staging.local

# ── Build & deploy artifacts (should never be in Git) ──
*.tar
*.tar.gz
*.patch
*.zip

# ── Analysis & report outputs ──
*.log
eslint_report.json
eslint_report.html
tsc-*.txt
tsc_output.txt
ts_errors.txt
test_output.txt
results.json
out.html
pin.html
*.html
*.png
*.txt

# ── Harness artifacts & reports ──
.antigravity/reports/
.antigravity/evidence/
.gemini/
.agent/
.agents/
.planning/audits/
teamwork_projects/
lovable_ui_spec.md
test8_*.js
test9_*.js
test-smtp.mjs
knowledge-service/data/indexer_state.json

# ── Data dumps (one-off, not source code) ──
stream-promotion-services.json
curl_out.txt
*.dump.json

# ── SQLite dev database ──
prisma/dev.db
prisma/*.db

# ── Playwright test results & artifacts ──
test-results/
playwright-report/
e2e/playwright/.auth/

# ── Coverage ──
coverage/
.nyc_output/

# ── OS-specific ──
.DS_Store
Thumbs.db

# ── IDE ──
.idea/
*.swp
*.swo

# ── Local patch artifacts ──
dist_patch/
