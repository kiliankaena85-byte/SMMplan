# 💻 Отчет об аудите: Дисциплина 10 (Tech Debt Engineering)
## Дата: 2026-05-12
## Фокус: Цикломатическая сложность, индексы БД, безопасность зависимостей

### 1. Архитектура и сложность кода (God Objects)
Были выявлены следующие файлы-гиганты с повышенной цикломатической сложностью (>400 строк):
- `src/components/landing/SmartLinkLanding.tsx` (1146 строк) — Требует декомпозиции UI-частей.
- `src/actions/order/checkout.ts` (620 строк) — Высокая сложность бизнес-логики чекаута, требует выделения сервисов.
- `src/components/orders/SmartOrderForm.tsx` (569 строк) — Стоит разбить на отдельные компоненты шагов формы.
- `src/services/providers/smart-analyzer.logic.ts` (431 строка) — Нормально для сложной бизнес-логики, но требует покрытия тестами.
- `src/services/admin/catalog.service.ts` (409 строк)

*Рекомендация:* Добавить эти файлы в бэклог рефакторинга для декомпозиции.

### 2. Зависимости и Supply Chain
Выполнен `npm audit`. Выявлено:
- 2 уязвимости (1 moderate в postcss <8.5.10, 1 high в next).
- Обновление напрямую через `npm audit fix` заблокировано из-за конфликта peer dependencies с React 19 (пакет `@studio-freight/react-lenis` требует `react-dom@17||18`).
- Типизация проекта находится в идеальном состоянии (`npx tsc --noEmit` завершился успешно).

*Рекомендация:* Ручное обновление `next` до патч-версии и `postcss`, когда зависимости будут поддерживать React 19.

### 3. База данных и индексы (schema.prisma)
Были оптимизированы индексы для предотвращения медленных запросов и table scans:
- **Удалены избыточные индексы:** 
  - `@@index([code])` в `PromoCode` (поле уже имеет `@unique`).
  - `@@index([token])` в `AuthToken` (поле уже имеет `@unique`).
- **Добавлен критический индекс:**
  - `@@index([serviceId])` в `Order` — добавлено для ускорения фильтрации и джоинов в админке, так как многие запросы ищут заказы конкретной услуги.

*Статус:* Изменения применены в `schema.prisma`. Требуется сгенерировать миграцию (`npx prisma migrate dev`).
