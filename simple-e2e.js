const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); await page.goto('http://127.0.0.1:3000/'); const content = await page.content(); if (content.includes('Каталог временно недоступен')) { console.error('E2E FAILED: Catalog is unavailable'); process.exit(1); } console.log('E2E PASSED: Catalog loaded successfully'); await browser.close(); })();
